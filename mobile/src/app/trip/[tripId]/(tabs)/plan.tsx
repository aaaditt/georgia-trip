import { TripTabPlaceholder } from '@/components/trip-tab-placeholder';

export default function PlanTab() {
  return (
    <TripTabPlaceholder
      emoji="📖"
      title="Plan"
      note="The read-only day-by-day itinerary lands alongside the calendar in Phase 3."
    />
  );
}
