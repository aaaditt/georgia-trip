import { TripTabPlaceholder } from '@/components/trip-tab-placeholder';

export default function NotesTab() {
  return (
    <TripTabPlaceholder
      emoji="📝"
      title="Notes"
      note="Shared trip + per-place notes land in Phase 4."
    />
  );
}
