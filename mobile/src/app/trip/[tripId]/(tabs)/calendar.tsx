import { TripTabPlaceholder } from '@/components/trip-tab-placeholder';

export default function CalendarTab() {
  return (
    <TripTabPlaceholder
      emoji="📅"
      title="Calendar"
      note="The touch-redesigned drag/resize itinerary grid lands in Phase 3."
    />
  );
}
