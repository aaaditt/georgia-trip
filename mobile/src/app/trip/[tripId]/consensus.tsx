import { TripTabPlaceholder } from '@/components/trip-tab-placeholder';

export default function ConsensusTab() {
  return (
    <TripTabPlaceholder
      emoji="🏆"
      title="Consensus"
      note="Ranked group favorites land in Phase 2."
    />
  );
}
