import { TripTabPlaceholder } from '@/components/trip-tab-placeholder';

export default function DashboardTab() {
  return (
    <TripTabPlaceholder
      emoji="🗺️"
      title="Dashboard"
      note="Region grid + voting progress lands in Phase 2."
    />
  );
}
